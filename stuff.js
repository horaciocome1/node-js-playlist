var counter = (arr) => { return `There are ${arr.length} elements` }

var adder = (a, b) => { return `The sum is ${a+b}` }

var pi = 3.142

module.exports = {
    counter: counter,
    adder: adder,
    pi: pi
}